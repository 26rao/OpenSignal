[project]
name = "opensignal"
version = "0.1.0"
description = "Self-healing long-tail opportunity monitor built with Bright Data Scraper Studio"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
